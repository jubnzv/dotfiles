#!/usr/bin/env python
# See: https://taskwarrior.org/docs/hooks.html
import json
import os
import sys

# Make no changes to the task, simply observe.
task = json.loads(sys.stdin.readline())
print(json.dumps(task))

os.system('pkill --signal USR2 taskboard')
