#!/usr/bin/env bash
set -euo pipefail

if [[ ! -x "$(command -v cmake)" ]]; then
    echo "cmake: command not found"
    exit 1
fi

if [ -d build ]; then
    rm -rI build
fi
mkdir build; cd build
cmake .. $@
