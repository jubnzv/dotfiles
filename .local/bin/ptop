#!/usr/bin/env bash
# Monitor process by name using top(1).
set -euo pipefail

if [[ ! -x "$(command -v top)" ]]; then
    echo "top: command not found"
    exit 1
fi
if [ $# -ne 1 ]; then
    echo "Usage: $0 <process name>"
    exit 1
fi

top -c -p $(pgrep -d',' -f $1)
