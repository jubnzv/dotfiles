# -*- mode: snippet -*-
# name: getopts_verbose
# key: getopts_verbose
# --
vison=0

while getopts "iv" option; do
case "$option" in
v)
echo "verbose mode"
vison=1 ;;
*)
exit 1 ;;
esac
done
shift $(( OPTIND - 1 ))

(( vison )) && echo 'Verbose mode'
$0
